# -----------------------------------------------------------------------------
# ha_yaml/pyheat_input_datetime.yaml
# Included via: input_datetime: !include_dir_merge_named yaml/input_datetime
# Symlink target: yaml/input_datetime/pyheat_input_datetime.yaml
# Contents: Reserved for future pyheat datetime inputs if needed.
# Note: Boiler anti-cycling now uses timer helpers (see pyheat_timers.yaml)
# -----------------------------------------------------------------------------

# No input_datetime entities currently needed
